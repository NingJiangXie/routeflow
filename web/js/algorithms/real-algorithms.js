/**
 * Algorithm Loader
 * Loads all individual algorithm modules
 */

// Export classes for backward compatibility
if (typeof window !== 'undefined') {
    // Algorithms are loaded separately via script tags in index.html
    console.log('Algorithm modules loaded separately');
}
